<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name="com.bridge.translator.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.BridgeTranslator">

        <activity
            android:name="com.example.bridgetranslator.OnboardingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.example.bridgetranslator.HomeActivity"
            android:exported="false" />

        <activity
            android:name="com.example.bridgetranslator.CameraActivity"
            android:exported="false" />

        <!-- Shape-aware live-scan camera with per-shape translation & TTS -->
        <activity
            android:name="com.example.bridgetranslator.CameraFeatureActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize" />

        <activity
            android:name="com.example.bridgetranslator.TranslateActivity"
            android:exported="false" />

        <activity
            android:name="com.example.bridgetranslator.HistoryActivity"
            android:exported="false" />

        <activity
            android:name="com.example.bridgetranslator.SettingsActivity"
            android:exported="false" />

        <activity
            android:name="com.bridge.translator.ui.main.MainActivity"
            android:exported="false" />

        <activity
            android:name="com.bridge.translator.ui.overlay.OverlayActivity"
            android:exported="false"
            android:theme="@style/Theme.BridgeTranslator.Overlay"
            android:taskAffinity=""
            android:excludeFromRecents="true" />

        <activity
            android:name="com.example.bridgetranslator.ScreenCapturePermissionActivity"
            android:exported="false"
            android:theme="@style/Theme.BridgeTranslator.Overlay"
            android:excludeFromRecents="true"
            android:taskAffinity="" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <service
            android:name="com.bridge.translator.service.FloatingBubbleService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <service
            android:name="com.bridge.translator.service.TranslatorAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_config" />
        </service>

        <service
            android:name="com.bridge.translator.service.BridgeAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/input_bridge_accessibility_config" />
        </service>

        <service
            android:name="com.bridge.translator.keyboard.BridgeKeyboardService"
            android:exported="true"
            android:label="@string/bridge_keyboard_name"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/bridge_keyboard_method" />
        </service>

        <activity
            android:name="com.bridge.translator.ui.LanguageSelectorActivity"
            android:exported="false" />

        <receiver
            android:name="com.example.bridgetranslator.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
